info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8081/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "email": "hhhau1910@gmail.com",
        "password": "1234567",
        "fullName": "Game"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
